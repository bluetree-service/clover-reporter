services:
  php82:
    image: chajr/php82-dev
    networks:
      - backend82
    command: ${RUN_COMMAND}
    environment:
      - MEMCACHED_SERVERS=memcached82
    volumes:
      - ${MOUNT_POINT}

networks:
  backend82:
    driver: bridge